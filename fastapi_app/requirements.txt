fastapi
uvicorn[standard]
httpx
pytest
pytest-asyncio
# create a fast api application with angular integration, it should have endpoint to serve to take orders(cash inflows) and cash outflows(expenses)
 

